# Production: API is same origin as the app, with the /api/v1 path prefix.
# Runtime will use window.location.origin when on hellowoco.app; this is fallback for build.
VITE_API_BASE_URL="https://hellowoco.app/api/v1"
VITE_API_URL="https://hellowoco.app/api/v1"
