# Production: API is same origin as the app (e.g. https://hellowoco.app/api).
# Runtime will use window.location.origin when on hellowoco.app; this is fallback for build.
VITE_API_BASE_URL="https://hellowoco.app"
