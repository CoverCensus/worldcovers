# On push to main: build frontend and optionally deploy.
# The built frontend (frontend/dist/) is produced so your server or deploy step can use it.
name: Build and deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Run migrations (check)
        run: python manage.py migrate --check --noinput || true

      - name: Build frontend
        run: |
          cd frontend
          npm ci
          npm run build

      - name: Upload frontend build
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/

  # Optional: add a deploy job that uses the artifact or triggers your host.
  # Uncomment and adjust for your host (e.g. Railway, Render, VPS via SSH).
  #
  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/download-artifact@v4
  #       with:
  #         name: frontend-dist
  #         path: frontend/dist/
  #     # Example: deploy to a server via SSH (you provide secrets).
  #     # - name: Deploy to server
  #     #   uses: appleboy/ssh-action@v1.0.0
  #     #   with:
  #     #     host: ${{ secrets.DEPLOY_HOST }}
  #     #     username: ${{ secrets.DEPLOY_USER }}
  #     #     key: ${{ secrets.DEPLOY_SSH_KEY }}
  #     #     script: |
  #     #       cd /var/www/worldcovers && git pull && ./scripts/deploy.sh && sudo systemctl restart worldcovers
